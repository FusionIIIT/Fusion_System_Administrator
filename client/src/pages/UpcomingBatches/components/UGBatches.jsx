import BatchSection from "./BatchSection";
import { PROGRAMME_CONFIG } from "../config/programmeConfig";

const UGBatches = (props) => <BatchSection config={PROGRAMME_CONFIG.ug} {...props} />;

export default UGBatches;
